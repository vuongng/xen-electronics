{
    "name": "xen-electronic",
    "description": "Xendit Electronic web app",
    "version": "1.0.0",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "cd api/ npm i && tsc -p . && node lib/server.js && cd ../web && sudo npm i && sudo npm run build && npm run start",
        "build-api": " cd api/ tsc -p . && npm run build",
        "publish": "",
        "build-web": "cd web/ next build"
    },
    "engines": {
        "node": "14.x"
    },
    "dependencies": {
        "next": "^10.1.3"
    }
}
