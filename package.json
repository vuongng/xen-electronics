{
    "name": "xen-electronic",
    "description": "Xendit Electronic web app",
    "version": "1.0.0",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "cd api/ tsc && node lib/server.js && cd ../../web && npm run start",
        "build": "",
        "publish": ""
    },
    "engines": {
        "node": "14.x"
    }
}
